<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h3>Minhas Metas - Perda de Peso</h3>
      <ul>
        <% @weight_goals.each do |weight_goal| %>
          <li>
            <%= link_to "#{weight_goal.desired}kg até #{weight_goal.deadline}", weight_goal_path(weight_goal) %>
          </li>
        <% end %>
      </ul>
      <%= link_to "Add a new weight goal", new_weight_goal_path, class: 'btn btn-primary' %>
    </div>
  </div>
</div>
